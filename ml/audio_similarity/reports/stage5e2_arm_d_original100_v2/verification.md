# Scope correction and verification

The owner requested the original frozen 100 only, not 100 queries against 741 candidates. Both similarity-matrix axes are restricted to the amended Stage5C2 100 source identities BEFORE ranking. Existing validated full matrices make inference unnecessary. The original Stage5E1 experiment and retired 741-track Stage5E2 artifacts remain unchanged.

The CLI now defaults to this versioned dataset. Existing retired review state is preserved and compatible numeric judgments can be reused by canonical pair and source identity. New mutable review state has a distinct ignored directory. Restart any previously running reviewer to load the new default.

Verification: 48 relevant tests passed, including explicit outsider exclusion, source mismatch rejection, deterministic subset ranking, canonical label reuse, blinding, and existing review/cache regressions. Frozen Stage5E1 inventory hashes verified during preparation. Source-validated review-store smoke check confirms the new pending queue.

Five-axis review: correctness restricts both axes and preserves frozen scores; architecture reuses matrix ranking and the existing reviewer without models or acquisition; readability isolates subset construction; security retains source hashes and local playback validation; performance loads only the necessary A/D matrices and performs no inference. No media or historical labels are changed.

This is still a D-focused queue, not an exhaustive A-versus-D judgment queue. A-only missing pairs are not added. Complete paired-query comparisons are reported only where both Top-5 pools have numeric ratings.
