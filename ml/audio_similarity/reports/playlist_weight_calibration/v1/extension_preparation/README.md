# Extension preparation — inventory checkpoint

Status: **FEATURE_READY=false; SPLIT_READY=false; READY_FOR_WEIGHT_SEARCH=false.**
This is an in-progress preparation checkpoint, not a calibration result.

The completed extension contains **17 source playlists**, **six credited curator
labels**, **1,314 membership slots**, and **1,169 recording requests**. Its twelve
acquisition batches are finished: 1,135 retained recording IDs, 24 manual-tail
requests and ten acquisition failures. Retained audio/provenance hashes pass.
Two retained IDs share identical source bytes/video, giving **1,134 unique audio
hashes**; both memberships and recording IDs remain identifiable.

There is no exact request/retained-ID/source-hash or conservatively linked version
overlap with the original 28-playlist corpus or its used development pilot.
Twelve extension recordings overlap earlier frozen retrieval evidence and stay
flagged as previously inspected. Four additional remix-family links were found
using the existing Tier-2 title/performer parser; they are conservative purge
links, not adjudicated equivalence.

## Draft roles

- Four Spotify genre lists are development-expansion candidates. Spotify already
  appears in the used pilot and contributes no new independent curator group.
- Eight Spotify Mix lists remain source-type-review/later-stress candidates, in
  the same development-only curator group; titles alone do not establish how
  they were curated.
- Five named non-Spotify curator groups are provisionally reserved: Actually
  Good House (Ben2424), Deep House 2026 (selected.), Drum and Bass Top 100 (UKF),
  actual hyperpop (chemical0), and synthpop synthwave kpop (Niedoes).

The draft uses source metadata and overlap only. It preserves the original
pilot samples and every source boundary. Confirmation geometry has 145 usable
sample recordings across five curator groups (30/29/30/30/26); failed or
previously inspected sampled recordings are not replaced. Eight sampled
and 20 full-development-catalog requests require version purges. This is a
small conditional confirmation proposal, below the retained resource targets;
source-use documentation, curator independence, prior inspection and broader
version adjudication remain unresolved. No split is activated or overwritten.

## Feature preparation and accounting

All 1,135 centered30 CLAP and MuQ caches passed the existing read-only verifier.
Twelve exact historical Method C caches are reused; 1,123 missing Method C
features are being computed by the unchanged single GPU worker, with its exact
pinned configuration and prior parity evidence. No new audio is downloaded.

No compatible exact-source Gemini profile was found for this extension.
Metadata-stripped local preparation for 1,135 profiles is in progress. Paid
execution is blocked pending extension-specific upload approval and a cap.
The historical-usage estimate is **$6.27**, with a proposed separate **$10 cap**.
The original run retains **$9.04152825 settled + $2.451456 unresolved exposure**;
its $15 approval does not cover the extension. Extension spend and calls are zero.

The two original approved profile deferrals remain unchanged. Missing extension
profiles are not valid unknown-genre profiles and must not receive fabricated
zero genre evidence. Confirmation feature catalogs remain separate from
selection inputs. No membership-recovery results were computed or inspected.

Private run: `.research_audio/playlist_calibration_extension_preparation_v1/`.
Current draft: `role_split_manifest_v3.private.json`; role catalogs:
`role_catalogs_v3/`. Restricted memberships, audio, recipes and cache ledgers
remain ignored. `inventory_milestone.json` records aggregate counts and hashes.
Focused existing calibration/cache/deduplication tests: **74 passed**. Final
matrix checks and zero-inference/API replay will follow completed extraction.
