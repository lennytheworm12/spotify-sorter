# Browser-facing base URL of the Express backend. Defaults to
# http://127.0.0.1:3000 when unset. Keep it on 127.0.0.1 (Spotify rejects
# `localhost` for local OAuth redirects); the backend listener bind host is a
# separate setting.
VITE_API_URL=http://127.0.0.1:3000
